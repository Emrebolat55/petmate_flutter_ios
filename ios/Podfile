# Official & Robust Podfile for Flutter
source 'https://cdn.cocoapods.org/'
platform :ios, '15.0'

require_relative '../Flutter/podhelper'

target 'Runner' do
  # linkage => :static resolves Ruby/CocoaPods internal validation bugs
  use_frameworks! :linkage => :static
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      # Force iOS 15.0 consistency
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      # Resolve Firebase/GoogleMaps non-modular head errors
      config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'
    end
  end
end
