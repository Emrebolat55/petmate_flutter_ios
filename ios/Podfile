# Final & Stable Podfile for Petmate
platform :ios, '15.0'

# Standard Flutter path relative to ios/Podfile
require_relative 'Flutter/podhelper'

target 'Runner' do
  # static linkage is critical for Firebase/Google Maps compatibility
  use_frameworks! :linkage => :static
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      # Force iOS 15.0 across all dependencies
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      # Resolve modular header conflicts
      config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'
    end
  end
end
